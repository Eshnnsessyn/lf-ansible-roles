#SPDX-License-Identifier: MIT-0
---
# tasks file for addsudoer
- name: add /etc/sudoers.d/ entry
  ansible.builtin.copy:
    dest: /etc/sudoers.d/{{ item.username }}
    content: '{{ item.username }} ALL=(ALL) NOPASSWD: ALL'
    owner: root
    group: root
    mode: '0440'
    validate: 'visudo -csf %s'
  loop: '{{ users }}'
  loop_control:
    label: "{{ item.username }}"
